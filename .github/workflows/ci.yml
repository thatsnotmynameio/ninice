name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  merge_group:
  push:
    branches: [main]

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  RUSTFLAGS: "-D warnings"
  RUSTDOCFLAGS: "-D warnings -D rustdoc::broken_intra_doc_links"

jobs:
  # Tier 0 — cheap source checks, no compile required. Gate the rest of CI.
  fmt:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    permissions:
      contents: read
    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-rust
        with:
          components: rustfmt
      - name: Check formatting
        run: cargo fmt --all --check

  typos:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    permissions:
      contents: read
    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - name: Run typos
        uses: crate-ci/typos@5374cbf686e897b15713110e233094e2874de7ef # v1.46.1

  rdme:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    permissions:
      contents: read
    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-rust
        with:
          tools: cargo-rdme
      - name: Check README is in sync
        run: cargo rdme --check

  # Tier 1 — first real compilation. Gates the heavy tier below so we don't
  # repeat the same compile error 4× across docs/msrv/hack/test.
  clippy:
    needs: [fmt, typos, rdme]
    runs-on: ubuntu-latest
    timeout-minutes: 3
    permissions:
      contents: read
    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-rust
        with:
          components: clippy
          cache-key: lint
      - name: Run clippy on all targets and features
        run: cargo clippy --all-targets --all-features -- -D warnings

  # Tier 2 — compile-heavy jobs. Run in parallel once clippy proves the
  # crate at least compiles cleanly.
  docs:
    needs: clippy
    runs-on: ubuntu-latest
    timeout-minutes: 3
    permissions:
      contents: read
    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-rust
        with:
          cache-key: lint
      - name: Build docs (no deps, all features)
        run: cargo doc --no-deps --all-features

  msrv:
    needs: clippy
    runs-on: ubuntu-latest
    timeout-minutes: 3
    permissions:
      contents: read
    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-rust
        with:
          tools: cargo-msrv,cargo-hack
          cache-key: msrv
      - name: Verify MSRV
        run: cargo msrv verify -- cargo check --all-features

  hack:
    needs: clippy
    runs-on: ubuntu-latest
    timeout-minutes: 3
    permissions:
      contents: read
    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-rust
        with:
          tools: cargo-hack
          cache-key: lint
      - name: Check feature powerset
        run: cargo hack check --feature-powerset --depth 2 --no-dev-deps

  test:
    needs: clippy
    runs-on: ubuntu-latest
    timeout-minutes: 3
    permissions:
      contents: read
    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-rust
        with:
          tools: cargo-nextest
          cache-key: test
      - name: Run unit tests with nextest
        run: cargo nextest run --all-features
      - name: Run doctests
        run: cargo test --doc --all-features

  # Tier 3 — coverage scan. Only meaningful if tests pass.
  sonarqube:
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 3
    permissions:
      contents: read
    # Sonar runs cargo clippy internally to collect findings; with the
    # workflow-level `RUSTFLAGS: -D warnings` every warning aborts the
    # scan instead of being reported. Reset it here so clippy completes
    # and the report reaches Sonar.
    env:
      RUSTFLAGS: ""
      RUSTDOCFLAGS: ""
    steps:
      - name: Checkout sources (full history for Sonar)
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
          persist-credentials: false
      - name: Install Rust toolchain with clippy and llvm-tools
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          components: clippy,llvm-tools-preview
      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@e1c4cd42111751368541a7cb5db3522bd1f846a4 # v2.78.0
        with:
          tool: cargo-llvm-cov
      - name: Restore cargo cache
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          shared-key: sonarqube
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - name: Generate LCOV coverage report
        run: cargo llvm-cov --lcov --output-path lcov.info --all-features
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@59db25f34e16620e48ab4bb9e4a5dce155cb5432 # v8.0.0
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  # Aggregator — required by branch protection. Every upstream must report
  # success; with the tiered `needs:` above, a `skipped` result now means an
  # upstream cascade failure, so we no longer accept `skipped` as OK.
  conclusion:
    needs: [fmt, typos, rdme, clippy, docs, msrv, hack, test, sonarqube]
    if: ${{ !cancelled() }}
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Verify all required checks succeeded
        run: |
          jq -e 'all(.[]; .result == "success")' \
            <<< '${{ toJSON(needs) }}'
